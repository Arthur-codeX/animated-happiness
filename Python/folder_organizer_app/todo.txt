1. Create the folder folder_organizer_app
   inside that folder create a file 
   called app.py 
   this will be the starting point.
2.Ask the user for a folder path
  -create function to handle this step
  main function:Starting:
  arrange your small function. 
3.Create a function get_user_folder:() 
   ->input:none:
   ->Output the chosen folder:
   ->prompt the user to enter folder 
   